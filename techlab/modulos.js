// function sumar(n,m){return n+m}

// function restar(n,m){return n-m}

// const multiplicar = (n,m) => {return n*m}

// export {sumar,restar,multiplicar}

const saludar = (nombre) => `Hola, ${nombre}`

const DB = ["teclado", "mouse", "pantalla"]

module.exports = {
    saludar,
    DB
}